export const SHEET_OBSERVABLE_V3 = Object.freeze({
  schemaVersion: 1,
  id: 'r11-profile-sheet-observable-v3-20260922',
  node: 'PROFILE-01',
  registeredAt: '2026-09-22',
  candidateSamplesObservedAtRegistration: false,
  baselineRevision: 'fe11daa407de396fad952be7679650f63dabd4dd',
  sourceBlobs: Object.freeze({
    behaviors: 'fa06548f592eddd7acd2c1121be26c1a86512718',
    gestures: 'a0b97661beece0ff1a7dce33203c8f921e4ac35d',
    slidingWindow: 'e1c3131a891ab9299b0746417d7e93ca98b190cc',
    decay: 'e6219f1d7d011375bbf93f699f659ebf80aeff7d',
  }),
  predecessor: Object.freeze({
    head: 'cc0d8891aa67de581b0b57a65a78ea7b3a80fa8c',
    run: 35702784754,
    artifact: 10682986570,
    artifactSha256: '3c4b4706b13215e237d9ec487f874ea61595c75fcec7aeb14db72724210231fb',
    falsifiers: Object.freeze([
      'phase-only continuation did not independently observe movement or velocity',
      'post-deadline sched.drain() could turn a terminal deadline miss into PASS',
      'host requestAnimationFrame timing left WebKit at value=260 and velocity=0 at the interruption observation',
    ]),
  }),
  premiseChange: Object.freeze({
    scheduler: 'inject deterministic virtual frame timestamps so the behavior law is separated from headless-host rAF latency',
    movementOracle: 'derive velocity only from two consecutive public value observations and the frozen frame delta; phase and state.velocity are not sufficient',
    terminalOracle: 'observe terminal state at the preregistered deadline with zero queued frame work; no drain or frame execution is allowed after the deadline',
    frozenInput: 'reuse the exact v2 pointer input, interruption time, target and terminal deadline; do not retime the scene from observed results',
  }),
  viewport: Object.freeze({ width: 390, height: 844 }),
  dpr: 2,
  snapPoints: Object.freeze([0, 300, 600]),
  frameStepMs: 16,
  timeline: Object.freeze([
    Object.freeze({ kind: 'down', atMs: 0, y: 0 }),
    Object.freeze({ kind: 'move', atMs: 160, y: 80 }),
    Object.freeze({ kind: 'move', atMs: 320, y: 180 }),
    Object.freeze({ kind: 'move', atMs: 480, y: 260 }),
    Object.freeze({ kind: 'up', atMs: 520, y: 300 }),
    Object.freeze({ kind: 'interrupt', atMs: 700, snapIndex: 1 }),
    Object.freeze({ kind: 'terminal-observation', atMs: 1000 }),
  ]),
  expected: Object.freeze({
    releaseInputVelocityPxPerSec: 1000,
    releaseTargetSnapIndex: 2,
    movementDirection: 1,
    terminal: Object.freeze({ phase: 'settle', value: 300, snapIndex: 1, velocity: 0 }),
  }),
  decision: Object.freeze({
    pass: 'all three desktop engines show finite non-zero forward movement from public value deltas before interruption, preserve value through snapTo(), and are terminal with an empty scheduler no later than 1000ms virtual time',
    fail: 'any engine has no independently observable movement, moves in the wrong direction, teleports on interruption, misses the terminal state, or still owns queued frame work at the deadline',
    retry: 'forbidden without a new independently justified premise-changing fact; no retiming or threshold tuning from this probe result',
  }),
});
