/**
 * internal/constants.ts — единые константы кадрового контура ядра
 * (drive / driver / motion-value). До выноса каждый файл нёс свою копию —
 * тюнинг одной без других разъезжался бы молча (coupled-дубль).
 *
 * Субпути (flip/gestures/keyframes/timeline/presets/frame) намеренно держат
 * СВОИ бюджеты: у них другая семантика (виртуальный прогон 100k кадров,
 * локальные лимиты жестов) — общая константа склеила бы независимые решения.
 */

/** Относительный порог сходимости (0.5% от range) — общий для позиции и скорости. */
export const CONVERGENCE_THRESHOLD = 0.005;

/** Потолок кадров одного запуска (~33.3 s при 60 fps) — страховка от вечного цикла. */
export const MAX_FRAMES = 2000;

/** Фиксированный шаг setTimeout-фоллбека (сек) при отсутствии rAF. */
export const FIXED_DT_S = 1 / 60;
